# Binaries (root-level only — /cmd/* directories must not be matched)
/errorprobe
/errorprobe.exe
/ep
/ep.exe
dist/

# Tilde backups (e.g. ep.exe~)
*~

# Vendor
vendor/

# Build artifacts
*.exe
*.out
*.test
cover
cover_*
coverage

# IDE
.idea/
.vscode/
*.swp
